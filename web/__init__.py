"""Web package for Library Management System.

This file makes the `web` folder a proper Python package so tools like mypy
and import machinery treat modules as `web.*` rather than top-level modules.
"""

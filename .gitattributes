# Preserve LF for shell scripts (used in CI)
*.sh text eol=lf
*.bash text eol=lf

# Preserve CRLF for Windows batch files
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Auto-detect text files
* text=auto
